export default {
  id: 'first-spring-boot-app',
  title: '318. First Spring Boot App',
  explanation: `Let us create and run your first **Spring Boot** application. Boot makes this a few-minute exercise.

**Step 1 — Generate the project.** Go to **start.spring.io** (Spring Initializr), or use your IDE's built-in wizard. Choose:
- **Project:** Maven, **Language:** Java, **Spring Boot:** the latest stable 3.x.
- **Group/Artifact:** e.g. \`com.example\` / \`demo\`.
- **Java version:** 17 or newer.
- **Dependencies:** add **Spring Web** (for a web app) or none for a plain console app.

Click **Generate**, unzip, and open the folder in your IDE.

**Step 2 — Look at the main class.** Boot creates a class annotated with \`@SpringBootApplication\` and a \`main\` method that calls \`SpringApplication.run(...)\`. That one line boots the whole framework: it starts the **IoC container** and, if Spring Web is present, an **embedded Tomcat** server.

**Step 3 — Run it.** Run the main class (or \`mvn spring-boot:run\`). You will see the Spring banner and, for a web app, a line like \`Tomcat started on port(s): 8080\`. Your app is live at \`http://localhost:8080\`.

**Step 4 — Add something to see.** Create a simple REST controller so a browser hit returns text.

**Understanding the structure:**
- \`src/main/java\` — your code (main class + controllers/services).
- \`src/main/resources/application.properties\` — configuration (e.g. \`server.port\`).
- \`pom.xml\` — dependencies and the Boot Maven plugin (which builds the runnable JAR).

That is the entire cycle: generate → run → add a controller. No XML, no external server.`,
  code: `// Main application class (generated by Initializr)
@SpringBootApplication
public class DemoApplication {
    public static void main(String[] args) {
        SpringApplication.run(DemoApplication.class, args);
    }
}

// A tiny controller so the browser sees something at http://localhost:8080/hello
@RestController
public class HelloController {
    @GetMapping("/hello")
    public String hello() {
        return "Hello from my first Spring Boot app!";
    }
}

/* Run DemoApplication -> console shows "Tomcat started on port(s): 8080"
   Visit http://localhost:8080/hello -> the returned text appears. */`,
  codeTitle: 'DemoApplication.java + a hello controller',
  points: [
    'Generate a starter project at start.spring.io (Maven, Java 17+, Spring Web dependency)',
    'The main class uses @SpringBootApplication and SpringApplication.run(...) to boot everything',
    'With Spring Web, an embedded Tomcat starts automatically on port 8080 — no external server',
    'application.properties holds config; pom.xml holds dependencies and the Boot Maven plugin',
    'Add a @RestController with a @GetMapping to return a response from a URL',
  ],
  callouts: [
    {
      type: 'tip',
      content: 'If port 8080 is already in use, change it by adding server.port=8081 to src/main/resources/application.properties. No code change and no restart of anything but the app is needed.',
    },
    {
      type: 'gotcha',
      content: 'Your controllers, services, and components must live in the same package as the @SpringBootApplication class or a sub-package of it. Component scanning starts from that package downward, so a class placed in a sibling or parent package will not be detected and you will get "no bean found" errors.',
    },
  ],
}
