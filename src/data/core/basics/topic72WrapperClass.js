export default {
  id: 'wrapper-class',
  title: '72. Wrapper Class',
  explanation: `**Wrapper classes** are object representations of Java's eight primitive types. They allow primitives to be used where objects are required (generics, collections, method parameters expecting Object).

**Primitive → Wrapper mapping:**
| Primitive | Wrapper |
|-----------|---------|
| \`int\` | \`Integer\` |
| \`double\` | \`Double\` |
| \`long\` | \`Long\` |
| \`float\` | \`Float\` |
| \`char\` | \`Character\` |
| \`boolean\` | \`Boolean\` |
| \`byte\` | \`Byte\` |
| \`short\` | \`Short\` |

All are in \`java.lang\` (auto-imported) and are \`final\` (immutable).

**Autoboxing and Unboxing (Java 5+):**
Java automatically converts between primitives and wrappers:
\`\`\`java
Integer i = 42;      // autoboxing: int → Integer
int n = i;           // unboxing: Integer → int
\`\`\`

**Integer cache:**
Java caches Integer values from **-128 to 127**. Two \`Integer\` objects with the same value in this range are the same object:
\`\`\`java
Integer a = 127; Integer b = 127; System.out.println(a == b); // TRUE (cached)
Integer c = 128; Integer d = 128; System.out.println(c == d); // FALSE (new objects)
\`\`\`

**Key utility methods:**
\`Integer.parseInt("42")\` → 42
\`Integer.toString(42)\` → "42"
\`Integer.MAX_VALUE\` → 2147483647
\`Integer.MIN_VALUE\` → -2147483648
\`Integer.compare(a, b)\`, \`Integer.sum(a, b)\``,
  code: `import java.util.*;

public class WrapperClass {
    public static void main(String[] args) {
        // Autoboxing: int → Integer (automatic)
        Integer a = 100; // autoboxed
        int b = a;       // unboxed

        // Generics require objects, not primitives
        List<Integer> nums = new ArrayList<>();
        nums.add(10); // autoboxed: int → Integer
        nums.add(20);
        int sum = 0;
        for (int n : nums) sum += n; // unboxed each iteration
        System.out.println("Sum: " + sum);

        // Integer cache: -128 to 127 are cached
        Integer x = 100, y = 100;
        System.out.println(x == y); // true (same cached object)
        Integer p = 200, q = 200;
        System.out.println(p == q); // false (different objects)
        System.out.println(p.equals(q)); // true (same value — always use equals)

        // String ↔ Primitive conversions
        int parsed = Integer.parseInt("42");
        double dbl = Double.parseDouble("3.14");
        String s = Integer.toString(99);
        System.out.println(parsed + " " + dbl + " " + s);

        // Constants
        System.out.println(Integer.MAX_VALUE); // 2147483647
        System.out.println(Double.MIN_VALUE);  // 4.9E-324 (smallest positive)

        // Null pitfall with unboxing
        Integer n = null;
        try {
            int v = n; // NullPointerException: unboxing null
        } catch (NullPointerException e) {
            System.out.println("Caught NPE from unboxing null Integer");
        }
    }
}`,
  codeTitle: 'Wrapper Classes — Autoboxing, Cache, Parse, and Null Pitfall',
  points: [
    'Wrapper classes box primitives into objects, required for generics (List<Integer>), Collections, and methods expecting Object',
    'Autoboxing (int → Integer) and unboxing (Integer → int) happen automatically since Java 5; generated by the compiler as valueOf() / intValue() calls',
    'Integer caches -128 to 127 — == comparison works for values in this range but fails outside; always use equals() for value comparison',
    'Integer.parseInt("42") converts String to int; Integer.toString(42) converts int to String — essential for I/O and JSON',
    'Unboxing a null wrapper (Integer n = null; int x = n) throws NullPointerException — a common and subtle bug',
    'All wrapper classes are final and immutable — you cannot subclass Integer or change its value once created',
  ],
  callouts: [
    {
      type: 'gotcha',
      content: 'The Integer cache trick in interviews: Integer a = 127; Integer b = 127; a == b is TRUE (cached). Integer a = 128; Integer b = 128; a == b is FALSE (new objects). ALWAYS use equals() to compare wrapper objects, never ==.',
    },
    {
      type: 'tip',
      content: 'Autoboxing in tight loops can cause significant GC pressure — each int → Integer creates a new heap object. For performance-critical numeric loops, keep primitives as primitive int arrays instead of List<Integer>.',
    },
    {
      type: 'interview',
      content: 'Q: What is the difference between int and Integer in Java?\nA: int is a primitive — stored directly on the stack or as an object field, no overhead. Integer is an object (wrapper) on the heap, capable of being null, usable in generics and collections, has helper methods like parseInt(). They auto-convert via boxing/unboxing.',
    },
  ],
}
